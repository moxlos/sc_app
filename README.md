# Supply - Demand optimisation for a supply chain network

- To build download the repository and run
```
docker build -t sc_app .
```

- When the image is ready run
```
sudo docker run --rm -p 8080:80 sc_app
```